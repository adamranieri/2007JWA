package dev.alsabea.daos;

import java.sql.Connection;

public interface CRUD <T> {
	
	T create(Connection con, T t) ;
	
	boolean delete(Connection con,int id);
	
	T retrieveById(Connection con,int id);
	
	T update (Connection con, T t);
	

}
